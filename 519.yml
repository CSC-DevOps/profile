- group:
    description: "Basic checks for 519"
    checks:
      - version:
         cmd: node --version
         range: ^10.x.x
      - version:
         cmd: git --version
         range: ^2.x.x
      - contains: 
         string: "@"
         file: ~/.gitconfig
