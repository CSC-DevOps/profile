- group:
   description: "Basic checks for 519"
   checks:
     - version:
         cmd: node --version
         range: ^10.x.x
     - version:
         cmd: git --version
         range: ^2.x.x
     # Email is configured in gitconfig
     - contains: 
         string: "@"
         file: ~/.gitconfig
# - group:
#    description: "Virtualization support and tools"
#    checks:
#      - capability:
#          memory: 4000
#          cores: 1
